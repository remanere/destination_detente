{% extends "base.html.twig" %}

{% block body %}

  <section>
  <div class="container py-5">
      <div class="row">
        <div class="col-12">
          <h3 class="text-center">{{ onglet.name }}</h3>
        </div>  

        {% for blogpost in onglet.blogposts %}

            <div class="col-sm-12 col-md-6 col-lg-4 mt-3">

                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">{{ blogpost.titre }}</h5>
                        <a href="{{ path('boutique_blogpost_detail',{'id': blogpost.id} ) }}" class="btn btn-primary w-100">Consulter</a>
                    </div>
                </div>
        
            </div>
            
        {% endfor %}

    </div>
     </div>
  </section>
   


{% endblock %}