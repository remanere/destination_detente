<nav class="navbar navbar-expand-lg navbar-light bg-light ">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('home') }}"><img class="logo" src="https://i.postimg.cc/W1FvF8v3/logo.png" alt='Destination Detente'></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">

      <ul class="navbar-nav me-auto mb-2 mb-lg-0 ">

        {% if app.user %}

            {% if app.user.roles[0] is same as "ROLE_ADMIN" %}
        
              <li class="nav-item">
                <a class="nav-link" aria-current="page" href="{{ path('admin_home') }}">Administration</a>
              </li>

            {% endif %}

            <li class="nav-item">
              <a class="nav-link" aria-current="page" href="{{ path('app_logout') }}">Se déconnecter</a>
            </li>

        {% else  %}

            <li class="nav-item">
              <a class="nav-link" aria-current="page" href="{{ path('app_login') }}">Se connecter</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" aria-current="page" href="{{ path('app_register') }}">S'inscrire</a>
            </li>

        {% endif %}        
          

    {% for onglet in ongletRepository.findAll() %}
          
            <li class="nav-item">
              <a class="nav-link" aria-current="page" href="{{ path('boutique_blogpost_detail',{'id':onglet.id}) }}">
                {{ onglet.name }}
              </a>
            </li>

        {% endfor %}


      </ul>
      
      <div class="d-flex">
        <a class="nav-link text-white" aria-current="page" href="#">
            Mon compte <i class="fas fa-user"></i>
          </a>
      </div>
      
    </div>
  </div>
</nav>