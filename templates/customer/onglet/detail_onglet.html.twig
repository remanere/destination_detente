{% extends 'customer/base.html.twig' %}

{% block title %}Onglet index{% endblock %}

{% block body %}
  <div class="container py-5">

    <div class="row">

    {% include "_messages_flash.html.twig" %}

        <section class="bg-secondary">
    <div class="container py-5">
    <div class="row">
          <div class="col-12">
            <h3 class="text-center text-white">Les onglets</h3>
          </div>  

          {% for onglet in onglets %}

              <div class="col-sm-12 col-md-6 col-lg-4 mt-3">

                  <div class="card">
                      <div class="card-body">
                          <h5 class="card-title">{{ category.name }}</h5>
                          <a href="{{ path('boutique_blogpost_show_by_onglet',{'id': onglet.id}) }}" class="btn btn-primary w-100">Consulter</a>
                      </div>
                  </div>
          
              </div>
              
          {% endfor %}

      </div>
    </div>
  </section>

    </div>

  </div>
{% endblock %}

