{% extends 'admin/base.html.twig' %}

{% block title %}Onglet{% endblock %}

{% block body %}
    <h1>Onglet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ onglet.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ onglet.name }}</td>
            </tr>
              <tr>
                <th>Image_path</th>
                <td>{{ onglet.image_path }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_onglet_index') }}">back to list</a>

    <a href="{{ path('admin_onglet_edit', {'id': onglet.id}) }}">edit</a>

    {{ include('admin/onglet/_delete_form.html.twig') }}
{% endblock %}
