{% extends 'admin/base.html.twig' %}

{% block title %}blogpost index{% endblock %}

{% block body %}
  <div class="container py-5">

    <div class="row">

        {% include "_messages_flash.html.twig" %}
    
        <div class="col-12 d-flex align-items-center justify-content-between">
            <h3>Liste des blogpost</h3>
            <a class="btn btn-primary" href="{{ path('admin_blogpost_new') }}">Ajouter</a>
        </div>

        <div class="navigation">
            {{ knp_pagination_render(blogposts) }}
        </div>
        {% for blogpost in blogposts %}

            <div class="col-sm-12 col-md-6 col-lg-4">

                <div class="card">
                 
                    <div class="card-body">
                        <h5 class="card-title">{{ blogpost.titre | upper }}</h5>
                        <p class="mb-1">{{ blogpost.onglet.name }}</p>
                        <a href="{{ path('admin_blogpost_edit',{ 'id' : blogpost.id } ) }}" class="btn btn-primary w-100">Gérer</a>
                    </div>
                </div>
        
            </div>
            
        {% endfor %}
         <div class="navigation mt-3">
            {{ knp_pagination_render(blogposts) }}
        </div>

    </div>

  </div>
{% endblock %}