{% extends 'base.html.twig' %}

{% block title %}Blogpost{% endblock %}

{% block body %}
    <h1>Blogpost</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ blogpost.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ blogpost.titre }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ blogpost.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_blogpost_index') }}">back to list</a>

    <a href="{{ path('admin_blogpost_edit', {'id': blogpost.id}) }}">edit</a>

    {{ include('/adminblogpost/_delete_form.html.twig') }}
{% endblock %}
